#include<iostream>
#include<string>
#include<vector>
using namespace std;
//«Æ¿Ã¡ﬂ



bool solution(vector<vector<int>> lands, vector<vector<int>> wells, vector<int> point) {
	bool answer = true;

	for (vector<vector<int>>::iterator itr = lands.begin(); itr != lands.end(); itr++) {

	}

	return answer;
}

int main() {

	ios_base::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);

	vector<vector<int>> cry;
	vector<vector<int>> cry2;
	vector<int> cry3;
	


	bool a = solution(cry, cry2, cry3);
	cout << a;
	system("pause");
	return 0;

}